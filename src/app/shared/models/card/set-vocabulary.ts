/** Token or helper object for the focused set. Not a catalog card. */
export interface SetVocabulary {
  id: string;
  setId: string;
  name: string;
  typeLine: string;
  oracleText: string;
  keywords: string[];
}
